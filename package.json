{
  "name": "mock-backend",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --watch db.json",
    "vercel-build": "echo 'No build step needed'"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "json-server": "^0.17.4"
  },
  "engines": {
    "node": "18.x"
  }
}
